import Layout from '../../shared/components/layout';

const Home = () => {
  return (
    <Layout pageTitle="Contato" menuActive={'contact'}>
      <h1>Contato</h1>
    </Layout>
  );
};

export default Home;
