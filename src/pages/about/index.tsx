import Layout from '../../shared/components/layout';

const Home = () => {
  return (
    <Layout pageTitle="Sobre" menuActive={'about'}>
      <h1>Sobre</h1>
    </Layout>
  );
};

export default Home;
