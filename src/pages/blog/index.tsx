import Layout from '../../shared/components/layout';

const Home = () => {
  return (
    <Layout pageTitle="Blog" menuActive={'blog'}>
      <h1>Blog</h1>
    </Layout>
  );
};

export default Home;
